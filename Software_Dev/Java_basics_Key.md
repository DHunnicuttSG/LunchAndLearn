## ✅ **Java Basics - Answer Key**

**1.** Edit → Compile → Execute  
**2.** Compiler translates entire code; interpreter executes line-by-line.  
**3.** `public static void main(String[] args)`  
**4.** Provides debugging, auto-complete, project management, error highlighting.  
**5.** Syntax = grammatical; Runtime = crash; Logic = incorrect results.  
**6.** Data = raw facts; Information = processed, meaningful data.  
**7.** `// single-line` and `/* multi-line */`  
**8.** Reserved keywords (`new`, `short`, `private`, `catch`, `this`) used as variable names.  
**9.** 5 → int; 128 → int; 34,780 → int; Bob → String; 86.02 → double; true → boolean; x → char  
**10.** Cast: `c = (float) a / b;`  
**11.** Input is `String`; use `Integer.parseInt()`, `Double.parseDouble()`, etc.  
**12.** Use breakpoints, step in/out/over, watch variables, use console.  
**13.** Output:
```
25Java
Java1312
```

**14.** Output:

```
12 is less than 13
12 is less than 13
This is all uppercase!
This one is equal
```

**15.** `if` handles complex boolean logic; `switch` works with discrete values.
**16.** `int n = (int)(Math.random() * 6) + 1;`
**17.** Output:

```
Both a and b are not true
either a or b is true
false
```

**18.**

```java
for (int i = 10; i >= 0; i--) {
    System.out.println(i);
}
System.out.println("Blast off!");
```

**19.** `while` checks before; `do/while` checks after executing once.
**20.**

```java
for(int i=1;i<=6;i++){
  for(int j=1;j<=i;j++){
    System.out.print(j + " ");
  }
  System.out.println();
}
```

**21.** “Don’t Repeat Yourself” — reuse code with methods/classes.
**22.**

```java
double areaRect(double w, double h){ return w * h; }
```

**23.**

```java
double areaTri(double b, double h){ return 0.5 * b * h; }
```

**24.** Scope defines where a variable can be accessed.
**25.** `final` means method can’t be overridden.
**26.** Ensures method overrides superclass method correctly.
**27.** Same name, different parameters.
**28.** Belongs to the class, not instance.
**29.** `int[] arr;` and `int[][] arr;`
**30.** `int[] arr = {1,2,3,4,5};`
**31.** Index beyond array length was accessed.
**32.** Element = stored value; Index = its position.
**33.** Fixed size, can’t grow dynamically.
**34.** Yes, arrays are objects in Java.
**35.**

```java
for(int val : arr){ System.out.println(val); }
```

**36.** When no references to an object remain.
**37.** Stack → local vars, method calls; Heap → objects, dynamic memory.
**38.** Used to include classes: `import java.util.*;`
**39.** A namespace for grouping related classes.
**40.** Tool to generate HTML documentation from comments.
**41.** Pass by value.
**42.** Value is copied into parameter; changes don’t affect original.
**43.** Reference (not in Java) means both refer to same memory location.
**44.** Occurs when trying to access a member of a null object.

---
