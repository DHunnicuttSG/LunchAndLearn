## 💻 **Git & GitHub — Interview Question Sheet**

---

1. What is source control?
2. What is a Git repository?
3. What does `git add --all` do?
4. What does `git commit -m "my comment"` do?
5. What does `git push` do?
6. What does `git pull` do?
7. Why would you create a branch in Git?
8. What does the `git clone` command do?

---
